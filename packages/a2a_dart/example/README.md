# A2A Dart Example

This example demonstrates a simple client-server interaction using the `a2a_dart` package.

## Running the Example

1.  **Start the server:**

    ```bash
    dart run bin/server.dart
    ```

2.  **Run the client:**

    ```bash
    dart run bin/client.dart
    ```
