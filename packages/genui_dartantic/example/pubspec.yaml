# Copyright 2025 The Flutter Authors.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

name: genui_dartantic_example
description: "genui_dartantic example app"
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.9.2 <4.0.0"
  flutter: ">=3.35.7 <4.0.0"

resolution: workspace

dependencies:
  dartantic_ai: ^1.3.0
  flutter:
    sdk: flutter
  genui: ^0.5.1
  genui_dartantic: ^0.5.1
  logging: ^1.3.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
