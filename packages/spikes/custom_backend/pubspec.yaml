# Copyright 2025 The Flutter Authors.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

name: custom_backend
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.9.0-333.2.beta

dependencies:
  dart_schema_builder:
    path: ../../dart_schema_builder  flutter:
  flutter:
    sdk: flutter
  flutter_genui:
    path: ../../flutter_genui

dev_dependencies:
  dart_flutter_team_lints: ^3.5.2
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
